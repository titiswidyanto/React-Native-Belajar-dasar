import React from 'react';
import {Text, View} from 'react-native';

const home = () => {
  return (
    <View>
      <Text />
    </View>
  );
};

export default home;

// const styles = StyleSheet.create({});
